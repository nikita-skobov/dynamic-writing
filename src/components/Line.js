import React from 'react'
import { connect } from 'react-redux'

export function Line(props) {
    return (
        <input type="text" defaultValue="dsadsadsa"></input>
    )
}

export default connect()(Line)
